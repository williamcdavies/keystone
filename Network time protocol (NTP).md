---
tags:
  - CPE400
---
NTP is a networking protocol for clock synchronisation between computer systems over packet-switched, variable-latency data networks.

NTP is intended to synchronise participating computers to within a few milliseconds of UTC.

Implementations send and receive timestamps using [[User datagram protocol (UDP)]]; the service is normally on port number $123$, and in some modes both sides us this port number.