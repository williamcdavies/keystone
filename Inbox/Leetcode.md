## Problems

### 1. Two Sum

```c++
class Solution {
	public:
		vector<int> twoSum(vector<int>& nums, int target) {
		unordered_map<size_t, int> u;
		
	for (size_t i = 0; i < nums.size(); ++i) {
		u.insert({i, nums[i]});
	}
	
	for (size_t i = 0; i < nums.size(); ++ i) {
	}
	}
};
```