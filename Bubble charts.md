---
tags:
  - CS491
---
A bubble chart is a type of chart that displays three dimensions of data. Each entity with its triplet $(v_1, v_2, V_3)$ of associated data is plotted as a disk that expresses two of the $v_i$ values through the disk's $(x, y)$ location and the third through tis size.