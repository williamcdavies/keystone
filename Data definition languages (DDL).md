---
tags:
  - CS457
---
A data definition language (DDL) is a syntax used for creating and modifying database objects such as tables, indices, and users.

`CREATE`, `ALTER`, `DROP`, `TRUNCATE`, and `RENAME` belong to DDL.