---
tags:
  - CS457
---
A data manipulation language (DML) is a syntax used for adding (inserting), deleting, and modifying (updating) data in a database.

`SELECT`, `INSERT`, `UPDATE`, and `DELETE` belong to DML.